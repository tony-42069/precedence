FROM node:18-slim

WORKDIR /app

# Copy package files from backend directory
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy the WebSocket service file
COPY websocket_service.js .

# Expose WebSocket port
EXPOSE 5003

# Start the service
CMD ["node", "websocket_service.js"]
